import { FocusEvent } from "react";
import { PickerData } from "../components/WheelPicker";
import { PickerItemRef } from "../types/pickerItemRef";
declare const useObserver: (data: PickerData[], selectedID: string, itemHeight: number, onChange: (data: PickerData) => void) => {
    root: import("react").MutableRefObject<HTMLUListElement | null>;
    refs: PickerItemRef;
    activeID: string;
    onFocus: (e: FocusEvent<HTMLLIElement>) => void;
};
export default useObserver;
